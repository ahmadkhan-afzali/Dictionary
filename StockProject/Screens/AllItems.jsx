import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const AllItems = () => {
  return (
    <View>
      <Text style={{color:'blue'}}>AllItems</Text>
    </View>
  )
}

export default AllItems

const styles = StyleSheet.create({})